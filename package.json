{
  "name": "sass-website",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "bootstrap-icons": "^1.0.0",
    "gh-pages": "^3.2.3",
    "node-sass": "^4.14.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.3"
  },

  "homepage": "https://emekah.github.io/avo",
  "scripts": {
    "start": "react-scripts start",
    "build": "VERSION=` git rev-parse --abbrev-ref HEAD` && react-scripts build && sed -1 -- 's/%VERSION%/$VERSION/g' build/index.html",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
